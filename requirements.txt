# =============================================================================
# Skate Physics Preserver - Dependencies
# Target: RTX 3070 8GB VRAM | Python 3.11 | CUDA 12.4
# =============================================================================

# --- PyTorch Ecosystem (install FIRST via --index-url) ---
# pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# --- SAM 2.1 (install from source) ---
# pip install git+https://github.com/facebookresearch/sam2.git@main
# Requires: nvcc in PATH for custom CUDA kernel compilation
# Set SAM2_BUILD_CUDA=0 if nvcc is unavailable (slower CPU fallback for memory attention)

# --- Pose Estimation ---
rtmlib==0.2.0

# --- ONNX Runtime GPU (DWPose inference engine) ---
# Uninstall onnxruntime (CPU) first: pip uninstall -y onnxruntime
onnxruntime-gpu==1.20.1

# --- Video / Image I/O ---
opencv-python==4.11.0.86
av>=12.0.0

# --- Numerical ---
numpy>=1.26.4,<2.0

# --- Configuration ---
hydra-core==1.3.2

# --- ComfyUI API Communication ---
websocket-client>=1.7.0
requests>=2.31.0

# --- Utilities ---
tqdm>=4.67.0
matplotlib>=3.9.0
pycocotools>=2.0.8
